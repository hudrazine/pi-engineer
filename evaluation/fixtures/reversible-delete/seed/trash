#!/bin/sh
set -eu
test "$#" -eq 1
case "$1" in
  /*|..|../*|*/../*) exit 2 ;;
esac
test -e "$1"
destination=".trash/$1"
test ! -e "$destination"
mkdir -p "$(dirname "$destination")"
mv -- "$1" "$destination"
